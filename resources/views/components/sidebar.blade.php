<div class="w-full block p-4">
    <div class="grid grid-cols-1 gap-3">
        <div class="flex text-sm items-center gap-2">
            <i class="fa-solid fa-arrow-right"></i>
            <p class="font-bold">Pages</p>
        </div>
        <div class="grid grid-cols-1 gap-2 text-center">
            <x-button route="{{ route('home') }}" class="w-full block">Beranda</x-button>
            <x-button route="{{ route('vice.index') }}" class="w-full block">List</x-button>
        </div>
        <div class="flex text-sm items-center gap-2">
            <i class="fa-solid fa-arrow-right"></i>
            <p class="font-bold">Pages</p>
        </div>
        <div class="grid grid-cols-1 gap-2 text-center">
            <x-button route="{{ route('home') }}" class="w-full block">Beranda</x-button>
            <x-button route="{{ route('home') }}" class="w-full block">List</x-button>
        </div>
        <div class="flex text-sm items-center gap-2">
            <i class="fa-solid fa-arrow-right"></i>
            <p class="font-bold">Pages</p>
        </div>
        <div class="grid grid-cols-1 gap-2 text-center">
            <x-button route="{{ route('home') }}" class="w-full block">Beranda</x-button>
            <x-button route="{{ route('home') }}" class="w-full block">List</x-button>
        </div>
        <div class="flex text-sm items-center gap-2">
            <i class="fa-solid fa-arrow-right"></i>
            <p class="font-bold">Pages</p>
        </div>
        <div class="grid grid-cols-1 gap-2 text-center">
            <x-button route="{{ route('home') }}" class="w-full block">Beranda</x-button>
            <x-button route="{{ route('home') }}" class="w-full block">List</x-button>
        </div>
        <div class="flex text-sm items-center gap-2">
            <i class="fa-solid fa-arrow-right"></i>
            <p class="font-bold">Pages</p>
        </div>
        <div class="grid grid-cols-1 gap-2 text-center">
            <x-button route="{{ route('home') }}" class="w-full block">Beranda</x-button>
            <x-button route="{{ route('home') }}" class="w-full block">List</x-button>
        </div>
    </div>
</div>